## What & why

<!-- What does this change and what problem does it solve? -->

## Checklist

- [ ] `ruff check src tests` and `ruff format src tests` pass
- [ ] `pytest` passes
- [ ] New deterministic logic lives in `metrics.py`/`trends.py` (not the prompt), with a test
- [ ] No real replays or secrets committed
- [ ] Updated `CHANGELOG.md` / docs if user-facing
