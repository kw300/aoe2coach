---
name: Bug report
about: Something didn't work as expected
title: "[bug] "
labels: bug
---

**What happened**
A clear description of the bug.

**Command / steps to reproduce**
```
aoe2coach ...
```

**Environment**
- OS:
- Python version:
- Backend installed: `full` (mgz) / `fast` (mgz-fast)
- aoe2coach version: (`aoe2coach --version`)

**Replay details (if parsing failed)**
- Game patch / build number (in the replay filename, e.g. `v101.103.44206.0`):
- Did `aoe2coach metrics <file>` also fail?

> ⚠️ Please don't attach real replays (they contain player names + Steam IDs). The
> build number above is usually enough — most parse failures are a new game patch.

**Error output**
```
paste here
```
